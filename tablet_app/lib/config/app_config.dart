/// No hardcoded backend URL. Backend is discovered on LAN or entered manually (backendApiBaseUrl in storage).
/// targetWebviewUrl always comes from backend config only.
const int kDefaultPollingIntervalSeconds = 25;
const int kConnectTimeoutSeconds = 15;
const int kReceiveTimeoutSeconds = 15;
